// const Redis = require('ioredis');
// const redis = new Redis(process.env.REDIS_URL);
// const getRedisConnection = () => {
//     try {
//         if (!redis) {
//             throw new Error('Redis connection undefined');
//         }
//         return redis;
//     } catch (error) {
//         throw error;
//     }
// };

// module.exports = { getRedisConnection };
